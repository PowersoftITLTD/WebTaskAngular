import { FilterCategoryPipePipe } from './filter-category-pipe.pipe';

describe('FilterCategoryPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCategoryPipePipe();
    expect(pipe).toBeTruthy();
  });
});
