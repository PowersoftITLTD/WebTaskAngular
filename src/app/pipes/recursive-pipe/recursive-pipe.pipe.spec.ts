import { RecursivePipePipe } from './recursive-pipe.pipe';

describe('RecursivePipePipe', () => {
  it('create an instance', () => {
    const pipe = new RecursivePipePipe();
    expect(pipe).toBeTruthy();
  });
});
